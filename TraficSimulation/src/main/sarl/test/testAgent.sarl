
package test

import io.sarl.core.Initialize
import ^agent.BehaviorFollowing
import utils.Converter
import ^agent.BehaviorArriving

agent Conducteur {
	on Initialize {
		var maxAccAgent : float = Converter.convertKHtoMS(10)
		var maxDesAgent : float = Converter.convertKHtoMS(10)
		var distanceFromObject : float = 20
		var currSpeed : float = Converter.convertKHtoMS(50)
		var idealSpeed : float = Converter.convertKHtoMS(50)
		var speedObject : float = 0
		System.out.println(maxAccAgent)
		System.out.println(maxDesAgent)
		System.out.println(distanceFromObject)
		System.out.println(currSpeed)
		System.out.println(idealSpeed)
		System.out.println(speedObject)
		//System.out.println(
		//	(new BehaviorFollowing()).computeIDM(maxAccAgent, maxDesAgent, distanceFromObject, currSpeed, idealSpeed,
			// speedObject))
		System.out.println( "FINAL RESULT : " + 
			(new BehaviorArriving()).computeIDM(maxAccAgent, maxDesAgent, distanceFromObject, currSpeed,
		 25, idealSpeed))
		
	}
}
